# 项目进度交接文档

> 最后更新：2026-09-04
> 用途：记录电赛E题（K230 视觉追踪 + 云台）项目进展，供后续会话/协作者快速接续工作。

---

## 一、项目概况

双芯片矩形靶标追踪系统（电赛E题）：

```
K230-CanMV(视觉) ──UART "dx,dy,dist,status\n"──> STM32F407(PID步进云台)
```

**最新分支方向**：K230 经 UART **直接控制 FashionStar 总线伺服舵机**（双轴舵机云台，不经 STM32），舵机驱动基于官网 C++ SDK 协议移植为 MicroPython。

---

## 二、已完成工作

### 阶段1：FashionStar 舵机 Skill 整合（已完成 ✅）

- 遍历 `云台/cpp-sdk/`（协议头文件、源码、13 个例程、CMake 工程）与官网 C++ SDK wiki、云台 PID 调节页
- 与用户确认需求定位：**庐山派 K230(CanMV/MicroPython) 经 UART 直控舵机**，C++ SDK 作为协议参考
- 产出 Skill（两处，内容同步）：
  - [.trae/skills/fashionstar-servo-k230/SKILL.md](file:///c:/Users/12553/Desktop/视觉/复刻/.trae/skills/fashionstar-servo-k230/SKILL.md) — 正式技能（Trae 可自动加载）
  - [skill/FASHIONSTAR-SERVO-K230-SKILL.md](file:///c:/Users/12553/Desktop/视觉/复刻/skill/FASHIONSTAR-SERVO-K230-SKILL.md) — 参考文档副本
- 内容：FSUS 协议精解（帧结构/校验和/25 条指令/参数地址/状态位/监控包）、MicroPython 驱动、双轴追踪框架、两点标定、保护参数、云台 PID 推荐值、排障清单、C++ SDK 对照表

### 阶段2：K230 舵机直控代码落地（已完成 ✅，未上板）

| 文件 | 改动 | 状态 |
|---|---|---|
| [k230/fs_servo.py](file:///c:/Users/12553/Desktop/视觉/复刻/k230/fs_servo.py) | **新增** MicroPython 舵机驱动（约330行）：FSServoBus/FSServo 类 + sync_set_angles() | ✅ 已验证 |
| [k230/config.json](file:///c:/Users/12553/Desktop/视觉/复刻/k230/config.json#L90-L118) | 新增 `servo` 配置段（20 参数，默认 `enabled: false`） | ✅ |
| [k230/main.py](file:///c:/Users/12553/Desktop/视觉/复刻/k230/main.py#L421-L485) | 注入 `servo_send`（与 uart_send 同构，lambda 注入模式）；CV/YOLO 共 5 个调用点挂载 | ✅ |
| [CLAUDE.md](file:///c:/Users/12553/Desktop/视觉/复刻/CLAUDE.md#L24-L27) / [README.md](file:///c:/Users/12553/Desktop/视觉/复刻/README.md#L47-L50) | 目录结构补录 fs_servo.py | ✅ |

### 验证结果（PC 端模拟）

- `py_compile`：fs_servo.py、main.py 均通过
- config.json：JSON 解析通过，servo 段 20 个键齐全
- **协议帧 mock 测试 9 项全部通过**：set_angle / sync 双轴帧 / ping 响应 / query_angle 负角度 / damping / stop(0x12标志) / monitor(电压/角度) / 多圈 / write_param —— 帧字节与 C++ SDK 逐字节一致（帧头 0x4C12、小端、0.1°单位、校验和%256、同步帧 3+n×7）

---

## 三、当前配置状态（重要）

[k230/config.json](file:///c:/Users/12553/Desktop/视觉/复刻/k230/config.json)：

- `uart.enabled = true`，port=2（GPIO5/6）→ 现有 STM32 链路仍开启
- `servo.enabled = false`，port=2（GPIO5/6）→ 舵机直控**默认关闭**
- ⚠️ **启用舵机时注意**：uart 与 servo 不能用同一 UART（启动有冲突告警）。方案二选一：
  1. 纯舵机方案：`uart.enabled=false` + `servo.enabled=true`（共用 UART2）
  2. 两路并存：舵机改 UART1（GPIO3/4，port=1, tx_pin=3, rx_pin=4）或 UART3（GPIO32/33，port=3）
- 舵机默认参数：yaw_id=0、pitch_id=1、yaw_k/pitch_k=0.02（度/像素）、pitch_reverse=true、限位 yaw±60°/pitch±30°、interval=40ms、deadband=5px、lost_hold=true、center_on_boot=false

---

## 四、待办 / 下一步（按顺序）

1. **上板接线**：TX(GPIO5) 串 ≈1kΩ 接舵机信号线、RX(GPIO6) 直连、共地、舵机独立供电；建议先用 UC-01 + 官方上位机验证舵机 ID/功能
2. **自检**：SD 卡放 main.py / config.json / fs_servo.py，config 开 `servo.enabled`，看启动日志 `servo: ... online`
3. **单舵机测试**：可用 fs_servo.py 的 ping/set_angle/query_angle/monitor 做最小验证（skill 第 3.1 节有代码）
4. **双轴追踪联调**：方向反了翻 `yaw_reverse/pitch_reverse`；跟不上加大 `yaw_k/pitch_k`（0.01~0.03 起）；振荡则减小 + 开 kalman(EMA)
5. **机械标定**（如需）：用 `calibration(rawA,realA,rawB,realB)` 两点标定，或 `set_origin()` 清零
6. **保护参数/PID**（负载变化时）：参考 skill 第 6 章，优先用上位机写入；舵机内部 PID 与 K230 视觉外环是两层控制，追踪效果主要调外环 k

## 五、已知约束与注意事项

- MicroPython 热路径规范：主循环每帧只发 1 帧同步写指令；ping/query/monitor 为阻塞调用（100ms 超时），**禁止进视觉主循环**
- 舵机总线为半双工单线：TX 必须串电阻防争抢；3.3V 电平通常可识别，不稳定需电平转换
- 协议中查询响应偶发校验错误（C++ SDK 亦容忍）；驱动写指令不等回复、查询指令带 retry
- C++ SDK 同步"指定转速"模式（子cmd12）转速字段疑似未 ×10 的 bug；MicroPython 驱动按协议单位 0.1°/s 实现
- K230 UART 引脚：UART1=GPIO3/4(Pin8/10)、UART2=GPIO5/6(Pin11/13)、UART3=GPIO32/33(Pin37/40)，需 FPIOA 映射
- 项目背景与既有功能（CV/YOLO 双模式、卡尔曼、降采样）见 [README.md](file:///c:/Users/12553/Desktop/视觉/复刻/README.md) 和 [CLAUDE.md](file:///c:/Users/12553/Desktop/视觉/复刻/CLAUDE.md)

## 六、关键资料索引

- 舵机 Skill 主文档：[SKILL.md](file:///c:/Users/12553/Desktop/视觉/复刻/.trae/skills/fashionstar-servo-k230/SKILL.md)（协议表/API/调参/排障全在里面）
- 协议权威源码：[FashionStar_UartServoProtocol.cpp](file:///c:/Users/12553/Desktop/视觉/复刻/云台/cpp-sdk/src/FashionStar_UartServoProtocol.cpp)
- C++ 例程目录：[云台/cpp-sdk/example](file:///c:/Users/12553/Desktop/视觉/复刻/云台/cpp-sdk/example)
- 官网：C++ SDK https://fashionstar.com.cn/wiki/sdk/servo/cpp-sdk/ ；云台PID https://fashionstar.com.cn/wiki/documents/servo/pid-tuning-guide/
