# 接线图 — K230 双轴云台

> 适用板卡：立创·庐山派 K230-CanMV（标准版 1GB）。引脚以 `skill/LUSHAN-PI-K230-PINOUT-SKILL.md` **第一节 40Pin 引脚图**为准。
> 配套图片：`云台/舵机接线图.png`（pitch 舵机）、`云台/TB6612_接线图.png`（旧直流电机方案，已弃用）。

## 一、总览

```
庐山派 K230 (标准版 1GB)  ·  3.3V 逻辑  ·  IO 不可接 5V
        │
        ├── UART3 TX/RX ─── GPIO32(Pin37) / GPIO33(Pin40)
        │        └──► Emm42_V5.0 闭环步进驱动器  →  42 步进电机   [左右 yaw 轴]
        │                  └── V+ ← 电源① 7~32V
        │
        ├── UART2 TX/RX ─── GPIO5(Pin11) 串 1kΩ / GPIO6(Pin13) 直连
        │        └──► FSUS 总线舵机 (ID=0)                        [上下 pitch 轴]
        │                  └── V+ ← 电源② 按舵机额定电压
        │
        └── GND ──────── Pin6/9/14/20/25/30/34/39
                          │
                          └── 与电源①的负极、电源②的负极 全部连通  ← 共地(必须)
```

- **左右轴 (yaw)** = Emm42_V5.0 闭环步进 + 42 步进电机，走 **UART3**（串口速度模式）
- **上下轴 (pitch)** = FashionStar FSUS 总线舵机，走 **UART2**（增量位置控制）

> ⚠️ **一个 UART 控制器只能有一组引脚。** 步进固定占 UART3、舵机固定占 UART2，不能互换或共用一个 `port`——配成同一个 UART 时会互相覆盖，程序启动时会打印 `WARN`。

## 二、K230 侧引脚清单（40Pin 排针）

| 物理脚 | GPIO | 本用途 | 接到哪里 |
|---|---|---|---|
| Pin 11 | GPIO5 | UART2_TXD | **串 1kΩ** → 舵机信号线 S |
| Pin 13 | GPIO6 | UART2_RXD | 直连 → 同一根舵机信号线 S |
| Pin 37 | GPIO32 | UART3_TXD | → Emm42 驱动器 **RXD** |
| Pin 40 | GPIO33 | UART3_RXD | → Emm42 驱动器 **TXD** |
| Pin 6 / 9 / 14 / 20 / 25 / 30 / 34 / 39 | — | GND | 与驱动器电源、舵机电源的 GND **全部共地** |

供电脚（本项目**不用**，两个执行器都独立供电）：

- 3.3V：Pin 1、Pin 17
- 5V：Pin 2、Pin 4（限流，带不动电机/舵机）

> **串口是交叉接**：K230 的 TX 接对方的 RX，K230 的 RX 接对方的 TX。
> **引脚以实测为准**：GPIO32(Pin37) 已经过万用表实测确认；GPIO33(Pin40) 依据 40Pin 引脚图，未单独实测——若 UART3 不通，先用 `k230/pin_probe.py` 复测。

## 三、yaw 轴：Emm42_V5.0 闭环步进

### 接线

| K230 | 驱动器 | 说明 |
|---|---|---|
| GPIO32 (Pin37) → | **RXD** | 交叉接 |
| GPIO33 (Pin40) → | **TXD** | 交叉接 |
| GND (任一 GND 脚) → | **GND** | 必须共地 |
| — | **V+** ← 独立电源正极 | 7~32V（**勿接 K230 的 5V**）|
| — | **GND** ← 独立电源负极 | 与 K230 GND 连通 |

- 驱动器与步进电机之间的相线（A+/A-、B+/B-）按驱动器丝印接好即可，**已由上位机验证过**
- 闭环（编码器）在驱动器内部完成，**K230 侧不需要接编码器**

### 驱动器侧必须预置（上位机 / OLED 菜单）

| 项 | 值 | 不设对的后果 |
|---|---|---|
| 校验方式 Checksum | **固定 0x6B** | 校验不对 → 驱动器直接丢弃指令，完全没反应 |
| 波特率 UartBaud | **115200** | 乱码 / 无应答 |
| 串口 ID_Addr | **1** | 地址对不上则不应答 |
| 运动模式 P_Pul / P_Serial | **UART_FUN**（串口功能） | 若是脉冲模式(P_Pul)或串口关闭(RxTx_OFF) → 收不到指令 |
| 应答 Response | **Receive** | 关掉则无法用"读电压"做在线探测 |

### 对应 config.json

```json
"motor": {
  "port": 3, "baud": 115200, "tx_pin": 32, "rx_pin": 33, "addr": 1
}
```

## 四、pitch 轴：FSUS 总线舵机

接线图见 **`云台/舵机接线图.png`**。这是**半双工单总线**，收发并到同一根信号线上。

### 接线

| K230 | 舵机 | 说明 |
|---|---|---|
| GPIO5 (Pin11) —**串 1kΩ**—→ | **信号线 S** | 1kΩ 只串在 TX 这一根上 |
| GPIO6 (Pin13) ————————→ | **信号线 S** | 直连；与上面接在**同一个节点** |
| GND (任一 GND 脚) → | **GND** | 必须共地 |
| — | **V+** ← 独立电源正极 | 按舵机额定电压（**勿接 K230 的 5V**）|

- 1kΩ 的作用：总线上 K230 和舵机可能同时驱动，串这个电阻限制对怼电流
- 舵机线色参考：**红 = V+，黑/棕 = GND，白/橙 = 信号**
- 总线 ID = **0**（`servo.pitch_id`），波特率 115200

### 对应 config.json

```json
"servo": {
  "enabled": true, "port": 2, "baud": 115200,
  "tx_pin": 5, "rx_pin": 6, "pitch_id": 0
}
```

## 五、电源与共地

三路电源，**互相独立但必须共地**：

| 电源 | 供给 | 电压 |
|---|---|---|
| K230 本体 | 板子自身 | Type-C 5V / 排针 5V / 8~24V 座 |
| 电源① | Emm42 驱动器 V+ | **7~32V**，电流按电机额定 |
| 电源② | 舵机 V+ | 按舵机额定电压 |

**共地是硬要求**：K230 的 GND、电源①的负极、电源②的负极必须连在一起。不共地会导致串口电平没有参考点，表现为"完全无应答"——本项目已经在这一点上栽过一次。

> 千万别用 K230 的 5V 去带电机或舵机：排针 5V 有限流，带不动，还会把板子电压拉垮导致重启。

## 六、上电自检

上电后看串口第一行 `actuator:`（正常长这样）：

```
actuator: yaw=emm(addr=1 UART3 TX=GPIO32 RX=GPIO33 1~30RPM rev=N) online 24000mV | pitch=servo(id=0 k=0.020 rev=N) online
```

| 字段 | 含义 |
|---|---|
| `online 24000mV` | 驱动器回包正常，`24000mV` 是驱动器母线电压（≈24V）——两个数都说明串口链路通 |
| `(no resp)` on yaw | 驱动器没回包 → 查 共地 / TX-RX 交叉 / 驱动器供电 / 上面那张驱动器的表 |
| `(no resp)` on pitch | 舵机没回包 → 查 共地 / 1kΩ 与 RX 是否并到同一信号线 / 舵机供电 / ID 是否为 0 |
| `!!! actuator init failed: ...` | 初始化抛异常，**两条轴都会一起哑掉**（`gimbal_send` 被降级成空操作）|

### 串口不通时的排障顺序

1. **看驱动器 OLED**：全黑 = 没上电；`Waiting V+ Power!` = V+ 电压不够；正常界面就进菜单核对第三、四节那两张表
2. **量共地**：万用表蜂鸣档，K230 GND ↔ 电源负极，必须导通
3. **回环测试**（最省事的一招，判断问题在 K230 侧还是外设侧）：
   拔掉外设，把该轴的 TX/RX 两个脚用杜邦线**短接**，跑 `motor.test_mode: true` 自检——
   - 打印 `RX 原始字节: 01 24 6B`（自己发的帧原样回来）→ **K230 侧正常**，问题在外设/接线
   - 打印 `RX 原始字节: (无)` → **K230 侧不通**，换串口引脚
4. **量 K230 侧引脚映射**：用 `k230/pin_probe.py` 逐个拉高 GPIO，万用表对照确定物理脚

## 七、调试历程备忘（避坑）

| 坑 | 教训 |
|---|---|
| UART1(GPIO3/4) 收发全无，短接回环也收不到 | GPIO3/4 默认是 JTAG 脚，UART1 属"借用"复用，实测不通 → 换 UART3 |
| 换了 UART2 原生引脚(GPIO5/6) 后 pitch 舵机被挤掉 | 一个 UART 控制器只能有一组引脚，最终 yaw 走 UART3、pitch 走 UART2 才不冲突 |
| 反复接错引脚 | **引脚一律以实测为准**（`pin_probe.py`），文档互相矛盾时信万用表 |
